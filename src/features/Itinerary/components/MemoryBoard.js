import React from 'react';
import './css/MemoryBoard.css';

const MemoryBoard = ({ itineraryId }) => {
    return (
        <div className="memory-board">
            <h2>Memory Board</h2>
            <p>Videos and Pictures will go here.</p>
        </div>
    );
};

export default MemoryBoard;
